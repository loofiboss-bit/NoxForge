# NoxForge 8.0.0-dev automated gate

This compact report records the active development line. It is not a release or
public-readback claim.

- Manifest, version synchronization, deterministic Store packages, package
  safety, and source-budget checks pass locally.
- The source archive excludes `AGENTS.md`, historical plans, full evidence,
  and future full matrices.
- Fresh 2560x1440 live input, physical cursor, audio routing, PAM/login, and
  power checks remain pending or blocked.
- Arch `makepkg`/`namcap` and external Fedora package lifecycle remain
  unverified until a stable candidate archive and matching host environments
  are available.

Version: 8.0.0-dev
Release state: development
